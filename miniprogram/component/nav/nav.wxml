<!-- 默认为黑色的返回键-->
<view class="nav" style='height: {{statusBarHeight + navBarHeight}}px;'>
  <view class="title-container">
    <!--  左上角的返回按钮 其中wx:if='{{navbarData.showCapsule}}' 是控制左上角按钮的显示隐藏，1为显示，0为隐藏 -->
    <view class='arrow' wx:if='{{navbarData.showCapsule}}' bindtap='_navback'></view>
     <!--  中间的标题 -->
    <view class='title'>{{navbarData.title}}</view>
  </view>
</view>